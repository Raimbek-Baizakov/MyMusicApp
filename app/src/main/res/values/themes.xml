<resources>
    <!-- Основная тема приложения -->
    <style name="Theme.MyMusicApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Цвет строки состояния -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Светлые или тёмные иконки в строке состояния -->
        <item name="android:windowLightStatusBar">false</item> <!-- Светлые иконки отключены -->
    </style>
</resources>
